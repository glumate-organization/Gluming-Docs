---
import { Image } from 'astro:assets';
import Button from '../components/Button.astro';
import CharacterCard from '../components/CharacterCard.astro';
import Layout from '../layouts/Layout.astro';
import ScreenCard from '../components/ScreenCard.astro';
import SectionHead from '../components/SectionHead.astro';
import Sprout from '../components/Sprout.astro';
import StoreButtons from '../components/StoreButtons.astro';
import { mobileAppSchema } from '../lib/seo';
import { characters, lifestyle, screens } from '../lib/assets';

// 홈은 앱 화면보다 "건강하게 · 함께 · 즐겁게"의 분위기를 먼저 보여준다.
// 라이프스타일 장면 3컷 — 사진 + 손글씨 톤의 한 줄(kicker) + 제목 + 짧은 글.
// 분위기가 겹치지 않게: 낮의 카페(확인) → 노을 산책(운동) → 부엌(식사).
const moments = [
  {
    img: lifestyle.cafeCheck,
    alt: '햇살 드는 야외 카페에서 샐러드 앞에 앉아 스마트폰을 보며 웃는 여성. 팔에 작은 혈당 센서가 붙어 있고, 옆 테이블에 새싹 마스코트 인형이 놓여 있다.',
    kicker: '지금 내 몸은 어떤 리듬일까요',
    title: '내 혈당의 변화를 쉽게 확인할 수 있어요',
    desc: '점심 한 그릇 앞에서 폰을 한 번 열어 보면 끝. 숫자를 해석하느라 애쓰지 않아도, 글루밍즈의 표정과 흐름 그래프가 오늘의 컨디션을 먼저 알려줘요.',
  },
  {
    img: lifestyle.coupleWalk,
    alt: '노을이 비치는 강변 산책로를 팔짱 끼고 걷는 중년 부부. 여성의 팔에 작은 혈당 센서가 붙어 있고 두 사람 모두 웃고 있다.',
    kicker: '지금, 더 건강한 내일을 위해',
    title: '함께 걷는 저녁 30분',
    desc: '검진 결과지를 받아 든 날, 막막함보다 먼저 손을 잡아요. 거창한 계획 대신 저녁마다 같이 걷기로 해요. 오늘 걸은 만큼 내 몸이 어떻게 답하는지, 글루밍이 살며시 보여드려요.',
  },
  {
    img: lifestyle.familyCooking,
    alt: '햇살이 드는 부엌에서 온 가족이 샐러드를 만드는 모습. 엄마의 팔에 작은 혈당 센서가 붙어 있고 아이들이 채소를 집으며 웃고 있다.',
    kicker: '같이 먹으니까 더 맛있고, 더 건강해요',
    title: '몸이 좋아하는 식단은 참는 식단이 아니에요',
    desc: '무엇을 먼저 먹을지 아이와 같이 고르는 작은 놀이. 먹기 전에 내 혈당 흐름을 한 번 그려보면, 식탁이 훨씬 즐거워져요.',
  },
];

// 앱은 뒤쪽에서 짧게 — 실제 화면 3컷만.
const appPeeks = [
  {
    img: screens.homeGood,
    title: '오늘 기분, 표정으로 톡',
    alt: '글루밍 홈 화면. 캐릭터가 웃으며 "리듬이 아주 좋아요, 이대로 가요!"라고 알려주고 혈당 109 mg/dL이 크게 보인다.',
    desc: '숫자만 봐선 좋은 건지 애매하죠? 글루밍즈가 표정 하나로 먼저 귀띔해 드려요.',
  },
  {
    img: screens.simExercise,
    title: '지금 움직이면 어떻게 될까요?',
    alt: '“어떤 운동을 해볼까요?” 화면. 빠른 걷기·스쿼트·런닝의 30분 뒤 예상 변화를 점선 기준선과 함께 비교해 보여준다.',
    desc: '가만히 있을 때와 움직였을 때를 나란히 견줘 봐요. 지난 7일 내 데이터로 계산한 참고값이에요.',
  },
  {
    img: screens.mealDetect,
    title: '찰칵 한 번이면 기록 끝',
    alt: '음식 인식 화면. 한 상 사진에서 음식 8개를 각각 잡아내고 총 탄수화물 136g·1,250kcal을 정리해 보여준다.',
    desc: '한 상을 찍기만 하면 음식이랑 탄수화물을 알아서 쏙 찾아내요. 먹기 전 흐름까지 미리 그려드려요.',
  },
];
---

<Layout
  title="글루밍 - 함께, 재미있게, 행복하게 혈당관리"
  description="글루밍은 CGM(연속혈당측정) 센서 값을 식사·운동 같은 하루 행동과 잇는 혈당 관리 습관 앱이에요. 가족과 걷고 같이 먹는 평범한 하루 안에서, 나의 혈당을 건강하게, 함께, 즐겁게 돌봐요."
  jsonLd={[mobileAppSchema()]}
>
  <!-- ── 1. HERO — 풀블리드 사진 위에 문구 (좌측 정렬) ────────── -->
  <section class="hero">
    <Image
      class="hero-bg"
      src={lifestyle.familyWalk}
      alt="노을 지는 강변 산책로를 손잡고 걷는 네 식구. 엄마의 팔에 작은 혈당 센서가 붙어 있고, 두 아이가 부모의 손을 잡고 웃고 있다."
      widths={[800, 1280, 1920]}
      sizes="100vw"
      loading="eager"
    />
    <div class="hero-shade" aria-hidden="true"></div>
    <div class="container hero-copy">
      <p class="eyebrow on-photo">A Wellness Companion</p>
      <h1 class="hero-title">
        함께, 재미있게, 행복하게<br />
        글루밍과 함께해요
      </h1>
      <p class="hero-lead">
        나의 혈당을 건강하게 돌보는 가장 다정한 방법. <br />
        가족과 걷고, 같이 먹고, 웃는 평범한 하루에 글루밍이 살며시 스며들어요.
      </p>
      <div class="hero-cta">
        <StoreButtons size="lg" tone="dark" />
      </div>
      <p class="hero-note">
        <a href="/features">앱은 어떻게 생겼는지 보기 →</a>
      </p>
    </div>
  </section>

  <!-- ── 2. 비의료 고지 ─────────────────────────────── -->
  <section class="trust">
    <div class="container trust-inner">
      <p class="trust-note">
        의료기기가 아니에요 · 생활습관 관리를 돕는 웰니스 서비스예요
      </p>
    </div>
  </section>

  <!-- ── 3. 일상의 장면들 ──────────────────────────────── -->
  <section class="section everyday">
    <div class="container">
      <SectionHead eyebrow="01 — Everyday" variant="split">
        거창한 결심 말고,<br />오늘 한 끼와 한 걸음부터
        <Fragment slot="sub">
          건강한 습관은 특별한 날이 아니라 평범한 저녁에 자라요.<br />
          같이 걷고, 같이 먹고, 내 몸이 어떻게 답하는지 함께 살펴보는 하루.
          글루밍이 그 옆에 있을게요.
        </Fragment>
      </SectionHead>

      <div class="moments">
        {
          moments.map((m) => (
            <article class="moment reveal">
              <div class="moment-shot">
                <Image
                  src={m.img}
                  alt={m.alt}
                  widths={[480, 800, 1200]}
                  sizes="(max-width: 640px) 90vw, (max-width: 980px) 45vw, 380px"
                  loading="lazy"
                />
              </div>
              <p class="moment-kicker">{m.kicker}</p>
              <h3 class="moment-title">{m.title}</h3>
              <p class="moment-desc">{m.desc}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ── 4. 문제 정의 ──────────────────────────────────── -->
  <section class="section problem">
    <div class="container">
      <SectionHead eyebrow="02 — Why Gluming" variant="split">
        숫자는 차곡차곡 쌓이는데,<br />뭘 바꿔야 할지는 막막하죠
        <Fragment slot="sub">
          이미 혈당이 오른 뒤에야 알림이 와서 그래프를 들여다봐요.<br />
          “아, 아까 그걸 먹지 말걸” 하고 아쉬워한 적, 한 번쯤 있지 않나요?
        </Fragment>
      </SectionHead>

      <div class="compare">
        <article class="compare-col is-before reveal">
          <span class="compare-tag">Before</span>
          <h3>오른 뒤에야 알게 돼요</h3>
          <svg class="mini" viewBox="0 0 260 96" role="img"
            aria-label="혈당이 목표 범위를 넘어 오른 뒤에야 확인하게 되는 흐름 그래프">
            <rect x="0" y="40" width="260" height="30" rx="6" class="mini-band" />
            <path d="M4 74 C 44 70 72 44 110 32 S 196 16 256 22" class="mini-line" />
            <circle cx="256" cy="22" r="3.6" class="mini-dot" />
            <text x="196" y="12" class="mini-cap">이미 지나감</text>
          </svg>
          <p>
            식사가 끝나고 혈당이 오른 다음에야 알림이 울려요.<br />
            그래프를 봐도 <strong>이미 지나간 일</strong>이라, 다음을 어떻게 바꿔야 할지
            혼자 고민하게 돼요.
          </p>
        </article>

        <article class="compare-col is-after reveal">
          <span class="compare-tag accent">Gluming</span>
          <h3>변화를 미리 살펴봐요</h3>
          <svg class="mini" viewBox="0 0 260 96" role="img"
            aria-label="지금 이후의 혈당 흐름을 점선으로 미리 그려 보여주는 그래프">
            <rect x="0" y="40" width="260" height="30" rx="6" class="mini-band is-on" />
            <line x1="112" y1="10" x2="112" y2="86" class="mini-now" />
            <path d="M4 70 C 40 66 76 58 112 56" class="mini-line is-on" />
            <path d="M112 56 C 148 52 176 46 206 50 S 242 58 256 52" class="mini-pred" />
            <circle cx="112" cy="56" r="3.6" class="mini-dot is-on" />
            <circle cx="256" cy="52" r="3.6" class="mini-dot-open" />
            <text x="118" y="16" class="mini-cap is-on">지금부터</text>
          </svg>
          <p>
            먹기 전에, 움직이기 전에 <strong>내 혈당이 어떻게 변할지</strong> 미리
            알려드려요.<br />
            무엇을 하면 좋을지까지 살며시 귀띔해 주니까, 오늘의 관리가 한결 쉬워져요.
          </p>
        </article>
      </div>
    </div>
  </section>

  <!-- ── 5. 작은 습관 — 콜라주 ─────────────────────────── -->
  <section class="section habits">
    <div class="container">
      <SectionHead eyebrow="03 — Small Habits" variant="split">
        작은 습관이<br />큰 변화를 만들어요
        <Fragment slot="sub">
          작은 센서 하나가 더 건강한 일상을 만들어요.<br />
          오늘은 몸이 좋아하는 식단으로, 내일은 함께 걷는 산책으로.
          글루밍은 그 순간들을 차곡차곡 기록하고, 다음 한 걸음을 미리 그려봐요.
        </Fragment>
      </SectionHead>

      <figure class="collage reveal">
        <Image
          src={lifestyle.momentsCollage}
          alt="일상 장면 7컷 콜라주. 노을 속 가족 산책 '오늘도 우리 가족의 건강한 하루', 팔에 붙인 센서 '작은 센서가 더 건강한 일상을 만들어요', 샐러드 준비 '오늘은 몸이 좋아하는 식단으로', 강아지와 산책 '함께 걷는 건강한 습관', 가족 식사 '같이 먹으니까 더 맛있고, 더 건강해요', 아침 스트레칭 '작은 습관이 큰 변화를 만들어요', 강변 벤치의 부부 '지금, 더 건강한 내일을 위해'."
          widths={[800, 1200, 1680]}
          sizes="(max-width: 1248px) 100vw, 1200px"
          loading="lazy"
        />
      </figure>
    </div>
  </section>

  <!-- ── 6. 앱은 뒤에서 조용히 ─────────────────────────── -->
  <section class="section features">
    <div class="container">
      <SectionHead eyebrow="04 — In the App" variant="split">
        복잡한 건 글루밍이,<br />선택은 언제나 나답게
        <Fragment slot="sub">
          앱은 조용히 뒤에서 거들어요. 표정으로 오늘을 알려주고, 행동 전에 흐름을
          미리 그려보고, 기록은 찰칵 한 번이면 끝.<br />
          아래는 모두 <strong>실제 앱 화면</strong>이에요.
        </Fragment>
      </SectionHead>

      <div class="cells feature-cells">
        {
          appPeeks.map((f, i) => (
            <ScreenCard
              img={f.img}
              title={f.title}
              alt={f.alt}
              index={String(i + 1).padStart(2, '0')}
            >
              {f.desc}
            </ScreenCard>
          ))
        }
      </div>

      <div class="cta-row">
        <Button href="/features" variant="secondary" size="md">
          기능 자세히 보기
        </Button>
      </div>
    </div>
  </section>

  <!-- ── 7. 글루밍즈 티저 ──────────────────────────────── -->
  <section class="section friends">
    <div class="container">
      <SectionHead eyebrow="05 — Meet the Glumings" variant="split">
        매일을 함께 걷는 친구들
        <Fragment slot="sub">
          기록도 습관도 혼자 하면 금세 지치잖아요.<br />
          글루밍즈가 옆에서 폴짝폴짝, 오늘의 작은 변화를 같이 응원할게요.
        </Fragment>
      </SectionHead>

      <div class="cells friend-cells">
        {characters.map((c) => <CharacterCard character={c} state="optimal" />)}
      </div>

      <div class="cta-row">
        <Button href="/characters" variant="secondary" size="md">
          글루밍즈 모두 만나기
        </Button>
      </div>
    </div>
  </section>

  <!-- ── 8. 최종 CTA ───────────────────────────────────── -->
  <section class="section cta-band-wrap">
    <div class="container">
      <div class="cta-band">
        <Sprout size={58} tile={false} class="cta-mascot" />
        <p class="eyebrow on-dark">Start Today</p>
        <h2 class="cta-title">오늘 저녁,<br />같이 걸어볼까요?</h2>
        <p class="cta-sub">
          행동하기 전에 내 혈당 흐름을 한 번 그려보는 습관.<br />
          함께라서 더 즐거운 하루를 글루밍과 시작해요.
        </p>
        <div class="cta-actions">
          <StoreButtons size="lg" tone="dark" />
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* ── HERO — 풀블리드 사진 + 어두운 그라데이션 + 흰 문구 ─── */
  .hero {
    position: relative;
    isolation: isolate;
    display: flex;
    align-items: flex-end;
    min-height: clamp(520px, 78vh, 800px);
    padding: clamp(56px, 8vw, 112px) 0 clamp(48px, 7vw, 88px);
    background: var(--pine);
    color: var(--on-dark);
    overflow: hidden;
  }
  .hero :global(.hero-bg) {
    position: absolute;
    inset: 0;
    z-index: -2;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 40%;
  }
  /* 왼쪽 아래를 짙게 — 문구가 놓이는 자리만 어둡고 오른쪽 위는 사진이 살아 있다 */
  .hero-shade {
    position: absolute;
    inset: 0;
    z-index: -1;
    background:
      linear-gradient(
        to top,
        rgba(20, 28, 22, 0.78) 0%,
        rgba(20, 28, 22, 0.42) 45%,
        rgba(20, 28, 22, 0.12) 100%
      ),
      linear-gradient(
        to right,
        rgba(20, 28, 22, 0.5) 0%,
        rgba(20, 28, 22, 0) 62%
      );
  }
  .hero-copy {
    position: relative;
  }
  .eyebrow.on-photo {
    color: var(--primary-light);
  }
  .hero-title {
    margin-top: var(--sp-1);
    max-width: 14ch;
    font-size: var(--fs-display);
    line-height: var(--lh-tight);
    letter-spacing: -0.045em;
    color: #ffffff;
    text-shadow: 0 2px 24px rgba(0, 0, 0, 0.28);
  }
  .hero-lead {
    margin-top: var(--sp-6);
    max-width: 560px;
    color: color-mix(in srgb, var(--on-dark) 88%, transparent);
    font-size: var(--fs-lead);
    line-height: 1.8;
    text-shadow: 0 1px 12px rgba(0, 0, 0, 0.3);
  }
  .hero-cta {
    margin-top: var(--sp-6);
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-3);
  }
  .hero-note {
    margin-top: var(--sp-5);
  }
  .hero-note a {
    color: var(--on-dark);
    font-weight: 600;
    font-size: var(--fs-body);
    border-bottom: 1px solid transparent;
    transition: border-color var(--dur-2) var(--ease);
  }
  .hero-note a:hover {
    border-bottom-color: currentColor;
  }

  /* ── 비의료 고지 스트립 ─────────────────────────────── */
  .trust {
    border-top: 1px solid var(--rule);
    border-bottom: 1px solid var(--rule);
    background: color-mix(in srgb, var(--warm) 45%, transparent);
  }
  .trust-inner {
    padding: var(--sp-4) 0;
  }
  .trust-note {
    margin: 0;
    color: var(--text-3);
    font-size: var(--fs-xs);
    font-weight: 600;
  }

  /* ── 일상의 장면들 ─────────────────────────────────── */
  .moments {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(20px, 3vw, 40px);
  }
  .moment {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--sp-3);
  }
  .moment-shot {
    width: 100%;
    border-radius: var(--r-lg);
    border: 1px solid var(--rule);
    overflow: hidden;
    background: var(--card-soft);
  }
  .moment-shot :global(img) {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 4 / 3;
    object-fit: cover;
  }
  .moment-kicker {
    margin-top: var(--sp-2);
    color: var(--sage-dark);
    font-size: var(--fs-sm);
    font-weight: 600;
  }
  .moment-title {
    font-size: var(--fs-h3);
    line-height: var(--lh-title);
    letter-spacing: -0.02em;
  }
  .moment-desc {
    color: var(--text-2);
    font-size: var(--fs-body);
    line-height: var(--lh-body);
    max-width: 40ch;
  }

  /* ── 문제 정의 ─────────────────────────────────────── */
  .compare {
    display: grid;
    grid-template-columns: 1fr 1fr;
    border-top: 1px solid var(--rule);
  }
  .compare-col {
    padding: var(--sp-7) clamp(24px, 4vw, 48px) var(--sp-6) 0;
    display: flex;
    flex-direction: column;
    gap: var(--sp-4);
  }
  .compare-col.is-after {
    padding-left: clamp(24px, 4vw, 48px);
    padding-right: 0;
    border-left: 1px solid var(--rule);
  }
  .compare-col h3 {
    font-size: var(--fs-h3);
  }
  .compare-col.is-before h3 {
    color: var(--text-3);
  }
  .compare-col.is-after h3 {
    color: var(--pine);
  }
  .compare-col p {
    color: var(--text-2);
    font-size: var(--fs-body);
    line-height: var(--lh-body);
    max-width: 44ch;
  }
  .compare-tag {
    font-family: var(--font-serif);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-3);
  }
  .compare-tag.accent {
    color: var(--sage-dark);
  }

  /* 미니 그래프 — 말로 하던 대비를 눈으로 */
  .mini {
    width: 100%;
    max-width: 340px;
    height: auto;
    display: block;
  }
  .mini-band {
    fill: color-mix(in srgb, var(--text-3) 12%, transparent);
  }
  .mini-band.is-on {
    fill: color-mix(in srgb, var(--primary) 15%, transparent);
  }
  .mini-line {
    fill: none;
    stroke: var(--text-3);
    stroke-width: 2.4;
    stroke-linecap: round;
  }
  .mini-line.is-on {
    stroke: var(--sage-dark);
  }
  .mini-pred {
    fill: none;
    stroke: var(--sage);
    stroke-width: 2.4;
    stroke-linecap: round;
    stroke-dasharray: 4 5;
  }
  .mini-now {
    stroke: var(--border-strong);
    stroke-width: 1;
    stroke-dasharray: 3 4;
  }
  .mini-dot {
    fill: var(--text-3);
  }
  .mini-dot.is-on {
    fill: var(--sage-dark);
  }
  .mini-dot-open {
    fill: var(--surface);
    stroke: var(--sage);
    stroke-width: 2;
  }
  .mini-cap {
    fill: var(--text-3);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.02em;
  }
  .mini-cap.is-on {
    fill: var(--sage-dark);
  }

  /* ── 콜라주 ────────────────────────────────────────── */
  .collage {
    margin: 0;
    border-radius: var(--r-lg);
    border: 1px solid var(--rule);
    overflow: hidden;
    background: var(--card-soft);
  }
  .collage :global(img) {
    display: block;
    width: 100%;
    height: auto;
  }

  /* ── 격자 칸 ───────────────────────────────────────── */
  .feature-cells {
    grid-template-columns: repeat(3, 1fr);
  }
  .friend-cells {
    grid-template-columns: repeat(4, 1fr);
  }
  /* 섹션 끝 버튼 — 본문과 같은 왼쪽 선에 맞춘다 */
  .cta-row {
    margin-top: var(--sp-7);
    display: flex;
    justify-content: flex-start;
  }

  /* ── 최종 CTA — 파인 다크 밴드 (좌측 정렬) ───────────── */
  .cta-band {
    position: relative;
    padding: clamp(56px, 8vw, 104px) clamp(28px, 5vw, 72px);
    border-radius: var(--r-lg);
    background: var(--pine);
    color: var(--on-dark);
    overflow: hidden;
    isolation: isolate;
  }
  /* 다크 면에도 같은 종이 질감을 얹어 인쇄물처럼 (배경 위 · 내용 아래) */
  .cta-band::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background-image: var(--grain);
    background-size: 180px 180px;
    opacity: 0.07;
    pointer-events: none;
  }
  /* 다크 면 위에서는 마스코트 몸(크림)이 그대로 잘 보여 타일이 필요 없다. */
  .cta-band :global(.cta-mascot) {
    display: block;
    margin: 0 0 var(--sp-4);
  }
  .eyebrow.on-dark {
    color: var(--primary-light);
  }
  .cta-title {
    font-size: var(--fs-h2);
    letter-spacing: -0.04em;
    line-height: var(--lh-title);
    color: var(--on-dark);
  }
  .cta-sub {
    margin: var(--sp-5) 0 var(--sp-7);
    max-width: 500px;
    font-size: var(--fs-lead);
    line-height: var(--lh-body);
    color: color-mix(in srgb, var(--on-dark) 78%, transparent);
  }
  .cta-actions {
    display: flex;
    justify-content: flex-start;
  }
  .cta-band :global(.btn-primary) {
    background: var(--on-dark);
    color: var(--pine);
  }
  .cta-band :global(.btn-primary:hover) {
    background: #ffffff;
  }

  /* ── 반응형 ────────────────────────────────────────── */
  @media (max-width: 980px) {
    .moments,
    .feature-cells,
    .friend-cells {
      grid-template-columns: repeat(2, 1fr);
    }
    .moment:last-child {
      grid-column: 1 / -1;
    }
    .moment:last-child :global(img) {
      aspect-ratio: 16 / 9;
    }
    .compare {
      grid-template-columns: 1fr;
    }
    .compare-col {
      padding: var(--sp-6) 0;
      border-bottom: 1px solid var(--rule);
    }
    .compare-col:last-child {
      border-bottom: 0;
    }
    .compare-col.is-after {
      padding-left: 0;
      border-left: 0;
    }
  }

  /* ── 모바일 — 축소판이 아니라 네이티브처럼 차분하게. 정렬은 계속 왼쪽 ── */
  @media (max-width: 640px) {
    .hero {
      min-height: clamp(520px, 86vh, 720px);
      padding: var(--sp-8) 0 var(--sp-7);
    }
    .hero :global(.hero-bg) {
      object-position: 42% 50%;
    }
    .hero-title {
      font-size: clamp(30px, 8.6vw, 42px);
      line-height: 1.12;
    }
    .hero-lead {
      margin-top: var(--sp-5);
      font-size: 16px;
    }
    .hero-lead br {
      display: none;
    }
    .hero-cta {
      margin-top: var(--sp-6);
    }
    .trust-inner {
      padding: var(--sp-5) 0;
    }
    .moments,
    .feature-cells,
    .friend-cells {
      grid-template-columns: 1fr;
    }
    .moment:last-child :global(img) {
      aspect-ratio: 4 / 3;
    }
    .cta-row {
      margin-top: var(--sp-6);
    }
    .cta-row :global(.btn) {
      width: 100%;
    }
  }
</style>
